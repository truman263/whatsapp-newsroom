# Workspace configuration package

Reserved for reusable workspace-level configuration when duplication creates a demonstrated need. Runtime API configuration currently remains owned by the API application.
